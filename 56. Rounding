# Write a program that rounds all the given numbers,
# separated by a single space, and prints the result as a list. Use round().


numbers = input().split()
final = []
for i in numbers:
    current = float(i)
    final.append(round(current))
print(final)
